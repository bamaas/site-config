# Site-specific values for podinfo
replicaCount: 1

ingress:
  enabled: true
  className: nginx  # @required "Kubernetes IngressClass name (e.g. nginx, traefik)" env:ZARF_VAR_INGRESS_CLASS
  host: asdfsadf.com  # @required "Ingress hostname for podinfo (e.g. podinfo.site.local)" env:ZARF_VAR_PODINFO_INGRESS_HOST
