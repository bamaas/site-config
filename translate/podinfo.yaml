# Site-specific values for podinfo
replicaCount: 1

ingress:
  enabled: true
  host: podinfo.poc.local  # @required "Ingress hostname for podinfo (e.g. podinfo.site.local)" env:ZARF_VAR_PODINFO_INGRESS_HOST
